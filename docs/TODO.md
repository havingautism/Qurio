2026-01-11：

1. 某个space上传文档后，切换space，文档会在另一个space出现

2. 文档上传时应该记录当前embedding模型，在会话中检索文档时要求使用同一embedding模型，否则提示错误

3. glm4.7模型在一些会话中。选中网络搜索或其他工具调用会直接返回done，没有正文，发生这个错误后重新生成或发送新问题均一直失败，llm觉得不调用任何工具时正常；但新会话glm4.7则一切正常，假设为某个会话上下文拼接格式或别的问题导致后端报错，返回前端显示done，后端目前没有错误日志，也许是捕获问题。
