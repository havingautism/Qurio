2026-01-11：

已解决：
1. 某个 space 上传文档后切换 space 时仍会显示旧文档（已在 ChatInterface 切换空间时清空并重新加载文档列表）。
2. 文档上传时未记录 embedding 模型，检索时没有校验模型一致性（已记录 embedding_provider/model 并在发送时强校验）。

待办：
1. glm4.7 模型在部分会话中调用工具直接返回 done 且无正文，后续触发一直失败；怀疑上下文拼接或后端未日志捕获导致。
